/**
 * Placeholder for print cron job.
 * In production, this would generate PDF files and queue them for printing.
 * Currently, printing is done through the browser's print dialog via the /print page.
 */
export function startPrintCronJob() {
  console.log("[Print Cron] Print cron job disabled — backend uses FastAPI");
}